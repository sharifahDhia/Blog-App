@* @model blogApp.Models.Blog

@{
    ViewData["Title"] = Model.Title;
}
<div class="form-container sign-in-container" style="margin-left: 25%; padding: 70px 0">


<h1>@Model.Title</h1>
<p>@Model.Content</p>

<hr />

<h3>Comments</h3>

<ul>
    @if(Model.Comments.Count() > 0)
    {
       @foreach (var comment in Model.Comments)
        {
            <li>
                <strong>@comment.CreatedAt.ToString("g")</strong> - @comment.Text
            </li>
        }
    }  
</ul>

<hr />
    <h4>Add a comment</h4>

    <form asp-action="AddComment" method="post">
        <input type="hidden" asp-for="id" />
        <div class="form-group">
            <textarea name="Text" class="form-control" placeholder="Write your comment here..."></textarea>
        </div>
        <div class="form-group">
            <input type="submit" value="Add Comment" class="btn btn-primary" />
        </div>
    </form>

</div>



 *@